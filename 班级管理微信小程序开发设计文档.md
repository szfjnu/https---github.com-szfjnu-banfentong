# 班级管理微信小程序 - 开发文档

## 1. 项目概述

本项目旨在开发一款功能全面的班级管理微信小程序，通过数字化手段提升班级管理的效率和透明度。小程序将服务于教师、学生、家长等不同角色，涵盖学生档案、德育积分、考勤、宿舍、成绩、竞赛、志愿服务等多个核心业务场景。

---

## 2. 核心功能模块

功能模块的设计严格遵循数据库 `DATABASE_SCHEMA_V2.md` 的定义。

### 2.1. 基础管理

-   **多角色权限系统 (`users`)**:
    -   支持管理员、教师、学生、家长等多种角色。
    -   不同角色拥有不同的操作权限和数据可见范围。
    -   通过微信 OpenID 实现用户身份的自动识别和登录。
-   **学期管理 (`semesters`)**:
    -   管理员可设置当前学期，所有数据将按学期进行归档和筛选。

### 2.2. 学生中心

-   **学生档案管理 (`students`)**:
    -   支持批量导入、单个录入、修改和查询学生详细信息。
    -   包含学生基本信息、家庭成员、个人简历、奖惩情况等。
-   **学生成绩管理 (`grade_records`)**:
    -   记录学生历次考试的各科成绩、总分和排名。
    -   支持成绩的录入、查询和统计分析。
-   **座位与分组管理 (`seating_charts`, `student_groups`)**:
    -   提供可视化的座位表编排功能。
    -   支持创建不同类型的学生分组（如学习小组、项目小组）。

### 2.3. 德育积分系统

-   **积分标准管理 (`score_items`)**:
    -   预设各类加分和扣分项目及其标准分值。
-   **积分记录与查询 (`score_records`)**:
    -   记录每一笔积分的变动，包括事由、日期、记录人等。
    -   学生和家长可实时查询积分明细和总分。
-   **积分兑换商城**:
    -   **物品管理 (`redemption_items`)**: 教师可上架可供兑换的实物奖品或虚拟权益。
    -   **兑换流程 (`redemption_requests`)**: 学生可使用积分兑换物品，教师进行审批和发放。

### 2.4. 日常管理

-   **考勤与请假管理**:
    -   **考勤记录 (`attendance_records`)**: 记录迟到、早退、旷课等情况。
    -   **请假审批 (`leave_requests`)**: 学生可在线提交请假申请，教师进行审批。
-   **宿舍管理**:
    -   **宿舍信息 (`dorms`)**: 管理宿舍楼栋、房间、床位等资源。
    -   **住宿积分与折算 (`dorm_score_conversion_rules`)**: 记录宿舍表现分，并按规则折算计入总德育分。
    -   **住宿预警 (`warnings`)**: 根据住宿积分自动触发“留宿察看”、“勒令退宿”等预警。
-   **处分管理 (`discipline_records`)**:
    -   记录学生的正式处分信息，并可关联相应的积分扣除。
-   **竞赛获奖管理 (`competition_records`)**:
    -   记录学生参加竞赛的获奖情况，并可关联相应的积分奖励。
-   **志愿服务管理 (`volunteer_records`)**:
    -   记录学生的志愿服务时长和内容，并可关联相应的积分奖励。

### 2.5. 数据与通知

-   **数据可视化仪表盘**:
    -   在首页为不同角色提供关键数据概览，如班级平均分、考勤异常数、待办事项等。
    -   提供多维度的数据分析图表（如成绩趋势、积分分布）。
-   **自动化通知与预警**:
    -   积分发生变动时，自动向学生和家长发送模板消息。
    -   当学生积分或宿舍分达到预设阈值时，自动触发预警并通知教师。

---

## 3. 技术栈

-   **前端**: 微信原生小程序
-   **UI组件库**: TDesign Weixin
-   **后端与数据库**: 微信云开发 (云函数、云数据库、云存储)
-   **数据可视化**: ECharts for Apache (微信小程序版)

---

## 4. 设计原则

-   **风格**: 遵循微信官方设计指导，采用 TDesign Weixin 组件库构建界面，确保原生体验的流畅性和一致性。
-   **布局**: 以卡片式布局为主，信息结构清晰，突出重点。
-   **交互**: 操作流程简化，提供明确的指引和及时的反馈，降低用户学习成本。
-   **色调**: 采用简洁、专业的色调，营造沉稳、信赖的视觉感受。

---

## 5. 开发计划

(✓: 已完成, ◐: 进行中, ○: 未开始)

### 阶段一：基础架构与核心功能

-   [○] 初始化原生小程序项目结构和云开发环境
-   [○] 配置 TDesign Weixin 组件库和全局样式
-   [○] 实现用户登录与多角色权限系统 (`users`)
-   [○] 完成所有云数据库集合的设计与创建 (`DATABASE_SCHEMA.md`)
-   [○] 开发首页仪表盘，实现关键数据聚合展示
-   [○] 开发学生信息管理模块 (`students`)，包括列表、详情、编辑
-   [○] 开发 Excel 导入/导出学生信息的云函数

### 阶段二：积分与考勤系统

-   [○] 开发积分管理系统 (标准 `score_items`, 记录 `score_records`)
-   [○] 开发考勤管理模块 (记录 `attendance_records`, 请假 `leave_requests`)

### 阶段三：扩展管理模块

-   [○] 开发宿舍管理功能 (信息 `dorms`, 规则 `dorm_score_conversion_rules`, 预警 `warnings`)
-   [○] 开发志愿服务管理模块 (`volunteer_records`)
-   [○] 开发处分管理模块 (`discipline_records`)
-   [○] 开发竞赛获奖管理模块 (`competition_records`)
-   [○] 开发学生成绩管理模块 (`grade_records`)

### 阶段四：高级功能与优化

-   [○] 开发积分兑换商城功能 (`redemption_items`, `redemption_requests`)
-   [○] 开发座位表与学生分组功能 (`seating_charts`, `student_groups`)
-   [○] 集成 ECharts 实现多维度数据可视化图表
-   [○] 开发个人中心模块（信息修改、密码设置等）
-   [○] 实现基于云函数的自动化通知系统
-   [○] 进行全面的功能测试、性能优化和兼容性适配