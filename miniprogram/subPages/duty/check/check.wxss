/* pages/duty/check/check.wxss */
.container {
  padding: 20rpx;
  background: #f5f7fa;
  min-height: 100vh;
}

.top-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}

.date-badge {
  padding: 12rpx 24rpx;
  background: white;
  border-radius: 20rpx;
  font-size: 28rpx;
  font-weight: 600;
  color: #1890ff;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
}

.batch-btn {
  padding: 12rpx 24rpx;
  background: #f6ffed;
  color: #52c41a;
  border-radius: 20rpx;
  font-size: 26rpx;
  border: 1rpx solid #b7eb8f;
}

/* 筛选标签 */
.filter-tabs {
  display: flex;
  gap: 0;
  margin-bottom: 20rpx;
  background: white;
  border-radius: 12rpx;
  overflow: hidden;
}

.filter-tab {
  flex: 1;
  text-align: center;
  padding: 18rpx 0;
  font-size: 26rpx;
  color: #666;
  border-bottom: 4rpx solid transparent;
}

.filter-tab.active {
  color: #1890ff;
  border-bottom-color: #1890ff;
  font-weight: 600;
}

/* 任务卡片 */
.task-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.task-card {
  background: white;
  border-radius: 16rpx;
  padding: 24rpx 30rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
}

.task-top {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.task-left {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.task-icon {
  font-size: 40rpx;
}

.task-info {
  display: flex;
  flex-direction: column;
}

.task-name {
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
}

.task-meta {
  font-size: 24rpx;
  color: #999;
  margin-top: 4rpx;
}

.status-tag {
  font-size: 24rpx;
  padding: 6rpx 16rpx;
  border-radius: 20rpx;
}

.status-tag.done {
  background: #f6ffed;
  color: #52c41a;
}

.status-tag.fail {
  background: #fff1f0;
  color: #ff4d4f;
}

.status-tag.pending {
  background: #fffbe6;
  color: #faad14;
}

/* 检查结果 */
.task-result {
  margin-top: 16rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid #f0f0f0;
}

.result-row {
  display: flex;
  gap: 8rpx;
  margin-bottom: 8rpx;
}

.result-label {
  font-size: 24rpx;
  color: #999;
  flex-shrink: 0;
}

.result-value {
  font-size: 24rpx;
  color: #333;
}

.result-value.plus { color: #52c41a; font-weight: 600; }
.result-value.minus { color: #ff4d4f; font-weight: 600; }

/* 检查按钮 */
.task-action {
  margin-top: 16rpx;
  padding-top: 16rpx;
  border-top: 1rpx solid #f0f0f0;
}

.check-btn {
  text-align: center;
  padding: 16rpx 0;
  background: #1890ff;
  color: white;
  border-radius: 12rpx;
  font-size: 28rpx;
  font-weight: 600;
}

.check-btn:active { opacity: 0.8; }

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 80rpx 0;
}

.empty-icon { font-size: 60rpx; margin-bottom: 16rpx; }
.empty-text { font-size: 26rpx; color: #999; }
.loading-state { text-align: center; padding: 60rpx 0; color: #999; }

/* 弹窗 */
.modal-mask {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.5);
  z-index: 999;
  display: flex;
  align-items: flex-end;
}

.modal-content {
  width: 100%;
  background: white;
  border-radius: 24rpx 24rpx 0 0;
  padding: 30rpx;
  max-height: 85vh;
  overflow-y: auto;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20rpx;
}

.modal-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #333;
}

.modal-close { font-size: 36rpx; color: #999; padding: 10rpx; }

.modal-task-info {
  background: #f5f7fa;
  padding: 16rpx 24rpx;
  border-radius: 12rpx;
  margin-bottom: 20rpx;
}

.modal-task-name {
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
  display: block;
}

.modal-task-student {
  font-size: 26rpx;
  color: #666;
  display: block;
  margin-top: 4rpx;
}

/* 合格按钮 */
.qualified-btns {
  display: flex;
  gap: 20rpx;
  margin-bottom: 24rpx;
}

.qualified-btn {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  border: 2rpx solid #d9d9d9;
  border-radius: 12rpx;
  font-size: 30rpx;
  color: #666;
}

.qualified-btn.active.yes {
  border-color: #52c41a;
  background: #f6ffed;
  color: #52c41a;
}

.qualified-btn.active.no {
  border-color: #ff4d4f;
  background: #fff1f0;
  color: #ff4d4f;
}

/* 表单 */
.form-group { margin-bottom: 20rpx; }
.form-group.half { flex: 1; }

.form-label {
  font-size: 26rpx;
  color: #666;
  margin-bottom: 10rpx;
  display: block;
}

.form-input {
  width: 100%;
  height: 72rpx;
  border: 1rpx solid #d9d9d9;
  border-radius: 8rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

.form-textarea {
  width: 100%;
  height: 100rpx;
  border: 1rpx solid #d9d9d9;
  border-radius: 8rpx;
  padding: 16rpx 20rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

.form-picker {
  height: 72rpx;
  line-height: 72rpx;
  border: 1rpx solid #d9d9d9;
  border-radius: 8rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  color: #333;
}

.form-row { display: flex; gap: 20rpx; }

.score-stars {
  display: flex;
  gap: 16rpx;
}

.star {
  font-size: 48rpx;
  color: #e8e8e8;
}

.star.active {
  color: #faad14;
}

.submit-btn {
  margin-top: 30rpx;
  margin-bottom: 40rpx;
  height: 88rpx;
  line-height: 88rpx;
  text-align: center;
  background: #1890ff;
  color: white;
  font-size: 32rpx;
  font-weight: 600;
  border-radius: 44rpx;
}
