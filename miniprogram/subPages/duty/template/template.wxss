/* pages/duty/template/template.wxss */
.container {
  padding: 20rpx;
  background: var(--background-color);
  min-height: 100vh;
}

.template-list {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.template-card {
  background: var(--white);
  border-radius: 16rpx;
  padding: 24rpx 30rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.04);
}

.template-header {
  display: flex;
  align-items: center;
}

.template-icon {
  font-size: 44rpx;
  margin-right: 16rpx;
}

.template-info {
  flex: 1;
  min-width: 0;
}

.template-name {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--text-color);
  display: block;
}

.template-category {
  font-size: 22rpx;
  color: var(--text-color-light);
  display: block;
  margin-top: 4rpx;
}

.template-actions {
  display: flex;
  gap: 16rpx;
}

.action-btn {
  font-size: 24rpx;
  padding: 8rpx 20rpx;
  border-radius: 8rpx;
}

.action-btn.edit {
  color: var(--primary-color);
  background: #e6f7ff;
}

.action-btn.delete {
  color: var(--error-color);
  background: #fff1f0;
}

.template-desc {
  font-size: 26rpx;
  color: var(--text-color-secondary);
  margin-top: 12rpx;
  padding-top: 12rpx;
  border-top: 1rpx solid var(--border-color);
}

.template-scores {
  display: flex;
  gap: 12rpx;
  margin-top: 16rpx;
  flex-wrap: wrap;
}

.score-tag {
  font-size: 22rpx;
  padding: 4rpx 14rpx;
  border-radius: 6rpx;
}

.score-tag.plus {
  color: var(--success-color);
  background: #f6ffed;
}

.score-tag.minus {
  color: #cf1322;
  background: #fff1f0;
}

.score-tag.inspect {
  color: #d48806;
  background: #fffbe6;
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 120rpx 0;
}

.empty-icon { font-size: 80rpx; margin-bottom: 20rpx; }
.empty-text { font-size: 30rpx; color: var(--text-color-light); }
.empty-hint { font-size: 24rpx; color: var(--text-color-light); margin-top: 10rpx; }

.loading {
  text-align: center;
  padding: 60rpx 0;
  color: var(--text-color-light);
}

/* FAB按钮 */
.fab-btn {
  position: fixed;
  right: 40rpx;
  bottom: 60rpx;
  width: 100rpx;
  height: 100rpx;
  border-radius: 50%;
  background: var(--primary-color);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 6rpx 20rpx rgba(24,144,255,0.4);
}

.fab-icon {
  font-size: 48rpx;
  color: var(--white);
  font-weight: 300;
}

/* 弹窗 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0,0,0,0.5);
  z-index: 999;
  display: flex;
  align-items: flex-end;
}

.modal-content {
  width: 100%;
  background: var(--white);
  border-radius: 24rpx 24rpx 0 0;
  padding: 30rpx;
  max-height: 85vh;
  overflow-y: auto;
}

.modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 30rpx;
}

.modal-title {
  font-size: 34rpx;
  font-weight: 600;
  color: var(--text-color);
}

.modal-close {
  font-size: 36rpx;
  color: var(--text-color-light);
  padding: 10rpx;
}

/* 表单 */
.form-group {
  margin-bottom: 24rpx;
}

.form-group.half {
  flex: 1;
  min-width: 0;
}

.form-label {
  font-size: 26rpx;
  color: var(--text-color-secondary);
  margin-bottom: 10rpx;
  display: block;
}

.form-input {
  width: 100%;
  height: 72rpx;
  border: 1rpx solid var(--border-color);
  border-radius: 8rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

.form-textarea {
  width: 100%;
  height: 120rpx;
  border: 1rpx solid var(--border-color);
  border-radius: 8rpx;
  padding: 16rpx 20rpx;
  font-size: 28rpx;
  box-sizing: border-box;
}

.form-picker {
  height: 72rpx;
  line-height: 72rpx;
  border: 1rpx solid var(--border-color);
  border-radius: 8rpx;
  padding: 0 20rpx;
  font-size: 28rpx;
  color: var(--text-color);
}

.form-row {
  display: flex;
  gap: 20rpx;
}

.icon-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.icon-item {
  width: 72rpx;
  height: 72rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  border: 2rpx solid var(--border-color);
  border-radius: 12rpx;
}

.icon-item.active {
  border-color: var(--primary-color);
  background: #e6f7ff;
}

.switch-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.form-actions {
  display: flex;
  gap: 20rpx;
  margin-top: 30rpx;
  padding-bottom: 40rpx;
}

.btn-cancel, .btn-confirm {
  flex: 1;
  min-width: 0;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  border-radius: 40rpx;
  font-size: 30rpx;
}

.btn-cancel {
  background: var(--border-color);
  color: var(--text-color-secondary);
}

.btn-confirm {
  background: var(--primary-color);
  color: var(--white);
}
