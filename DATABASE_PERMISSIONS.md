# 数据库权限配置说明

## 问题说明

### 问题1：登录时用户信息更新失败
users集合设置为"仅创建者可读写"权限,导致更新用户信息失败。

### 问题2：班级加入功能跨用户查询失败（核心问题）
当 `classes`、`students`、`user_class_relation` 等集合设置为"仅创建者可读写"时，
非创建者用户无法查询到别人创建的数据，导致：
- 通过班级码/手机号搜索班级 → 返回空结果
- 查看班级详情 → 权限被拒绝
- 查看班级学生列表 → 返回空结果
- 欢迎页加载已加入班级 → 获取班级详情失败

**根因**：微信云开发安全规则会在查询时自动添加 `_openid == 当前用户openid` 过滤，
导致非创建者只能看到自己创建的文档。

## 解决方案

### 方案1: 修改数据库权限(简单但安全性较低)

在云开发控制台修改集合的权限设置:

1. 打开云开发控制台
2. 进入数据库 → 对应集合
3. 点击"权限设置"
4. 修改为 **所有用户可读,仅创建者可写**

适用集合:
- `classes`: 所有用户可读（搜索班级需要）
- `students`: 所有用户可读（加入班级需要查看学生）
- `user_class_relation`: 所有用户可读（查询自己的班级关系）
- `users`: 所有用户可读,仅创建者可写

### 方案2: 使用云函数操作数据库(推荐，已实现 ✅)

创建 `joinClass` 云函数，云函数以管理员身份运行，绕过安全规则限制。
已实现以下操作：

| 操作 | 说明 |
|------|------|
| searchByCode | 通过班级码查找班级 |
| searchByPhone | 通过手机号查找班级 |
| getClassDetail | 获取班级详情（含学生数量） |
| getStudents | 获取班级学生列表 |
| checkUserInClass | 检查用户是否已加入班级 |
| joinClass | 加入班级（创建关系记录+可选创建学生） |
| getUserClasses | 获取用户已加入的班级列表 |

### 方案3: 代码层面处理(已实现)

在login.js中已经做了容错处理:
- 数据库更新失败不会阻塞登录流程
- 用户信息会保存到本地存储
- 登录成功后会正常跳转

## 已修改的文件

| 文件 | 修改内容 |
|------|----------|
| `cloudfunctions/joinClass/index.js` | 新建云函数，处理班级加入相关操作 |
| `cloudfunctions/joinClass/package.json` | 云函数配置 |
| `pages/join/join.js` | 搜索班级改用云函数 |
| `pages/join/info/info.js` | 加载班级详情改用云函数 |
| `pages/join/bind/bind.js` | 加载学生列表改用云函数 |
| `pages/join/confirm/confirm.js` | 加入班级操作改用云函数 |
| `pages/welcome/welcome.js` | 加载用户班级列表改用云函数 |

## 数据库权限建议

### 开发环境
- users: 所有用户可读写
- classes: 所有用户可读写
- students: 所有用户可读写
- user_class_relation: 所有用户可读写
- score_items: 所有用户可读写
- semesters: 所有用户可读写

### 生产环境
- users: 所有用户可读,仅创建者可写
- classes: 所有用户可读,仅创建者可写
- students: 所有用户可读,仅创建者可写
- user_class_relation: 所有用户可读,仅创建者可写
- score_items: 所有用户可读,仅管理员可写
- semesters: 所有用户可读,仅管理员可写

## 部署步骤

1. **上传云函数**：在微信开发者工具中右键 `cloudfunctions/joinClass` → 上传并部署
2. **（可选）修改数据库权限**：在云开发控制台修改相关集合权限
3. **重新编译小程序**：确保代码更新生效
4. **测试验证**：使用不同账号测试班级搜索和加入功能

## 注意事项

1. **权限设置**: 确保数据库权限设置正确
2. **云函数部署**: joinClass云函数必须上传部署后才能使用
3. **索引创建**: 根据云开发建议创建索引
4. **错误处理**: 代码中已做容错处理
5. **测试验证**: 修改权限后重新测试登录和加入流程
