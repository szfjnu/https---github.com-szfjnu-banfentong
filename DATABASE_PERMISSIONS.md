# 数据库权限配置说明

## 问题说明

在测试登录时发现,users集合设置为"仅创建者可读写"权限,导致更新用户信息失败。

## 解决方案

### 方案1: 修改数据库权限(推荐)

在云开发控制台修改users集合的权限设置:

1. 打开云开发控制台
2. 进入数据库 → users集合
3. 点击"权限设置"
4. 修改为以下之一:
   - **所有用户可读,仅创建者可写** (推荐)
   - **所有用户可读写** (开发环境可用)

### 方案2: 使用云函数操作数据库

如果需要更严格的权限控制,可以通过云函数来操作数据库:

1. 创建一个updateUser云函数
2. 在云函数中更新用户信息
3. 云函数有管理员权限,可以绕过权限限制

### 方案3: 代码层面处理(已实现)

在login.js中已经做了容错处理:
- 数据库更新失败不会阻塞登录流程
- 用户信息会保存到本地存储
- 登录成功后会正常跳转

## 当前实现

代码已经优化为:
1. **不阻塞登录**: 数据库操作失败不影响登录
2. **立即跳转**: 移除setTimeout,登录成功后立即跳转
3. **容错处理**: 跳转失败时尝试使用reLaunch
4. **错误日志**: 记录错误但不中断流程

## 测试步骤

1. 修改users集合权限为"所有用户可读,仅创建者可写"
2. 重新编译小程序
3. 点击登录按钮
4. 授权用户信息
5. 应该能正常跳转到首页

## 数据库权限建议

### 开发环境
- users: 所有用户可读写
- students: 所有用户可读写
- score_items: 所有用户可读写
- semesters: 所有用户可读写

### 生产环境
- users: 所有用户可读,仅创建者可写
- students: 所有用户可读,仅创建者可写
- score_items: 所有用户可读,仅管理员可写
- semesters: 所有用户可读,仅管理员可写

## 注意事项

1. **权限设置**: 确保数据库权限设置正确
2. **索引创建**: 根据云开发建议创建索引
3. **错误处理**: 代码中已做容错处理
4. **测试验证**: 修改权限后重新测试登录流程
